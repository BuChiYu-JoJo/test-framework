# Dataify 实名认证模块 PRD（前端开发专用版）

**文档版本**：V1.0

**适用端**：Web 端

**对接角色**：前端开发工程师、测试工程师、UI设计师

**状态**：正式开发需求

**核心说明**：本PRD仅聚焦前端开发相关需求，剔除后端接口实现细节，仅预留接口调用占位；重点明确页面结构、前端校验、交互逻辑、视觉要求及开发注意事项，个人认证对接腾讯云（前端仅负责接口调用、页面渲染及交互，不涉及后端接口封装），扫码核验由微信/支付宝承接，企业认证前端流程保持不变，确保前端人员可直接对照开发。

---

# 一、前端开发核心前提

1.  接口调用：所有接口仅预留调用地址、请求方式及返回字段示例，前端按占位格式调用，后端接口调试完成后直接替换，无需修改前端调用逻辑；

2.  兼容性：兼容主流浏览器（Chrome、Edge、Firefox最新版本），适配1920*1080、1366*768分辨率，响应式适配平板设备（最小宽度768px）；

3.  交互规范：所有弹窗、按钮、表单交互统一遵循平台现有前端交互规范，无特殊说明均按默认规范执行；

4.  错误处理：所有接口调用失败、校验失败需按指定文案提示，禁止自定义提示文案，确保用户体验统一；

5.  腾讯云对接：前端仅负责触发接口调用、渲染腾讯云返回的二维码、轮询核验结果，不涉及腾讯云密钥配置及接口封装。

---

# 二、页面结构及前端开发详细需求（按开发优先级排序）

## Page1 实名认证入口页（前端核心开发项）

### 页面说明

用户进入「个人中心 → 设置 → 账户与安全 → 实名认证」进入该页面，为个人/企业认证的入口枢纽，前端需先查询认证状态，动态渲染页面内容。

### 界面结构（严格对照UI设计稿，核心开发点）

- 顶部标题：实名认证（字体、颜色、间距按UI规范，不可随意调整）；

- 选项卡：个人认证、企业认证（默认选中个人认证，切换时无加载动画，瞬间切换状态）；

- 操作入口：点击对应选项卡后，显示【去认证】按钮（按钮样式按平台主按钮规范，hover状态、点击状态需完整实现）；

- 底部提示：固定文案（如下），换行、间距按UI设计稿，不可修改文案内容：

为什么我们要您验证您的身份？

身份认证对于以下几个方面至关重要：

1. 服务品质与资源管理：通过使用者身份管理资源，确保服务稳定可靠；帮助提供更好的客户支援并解决技术、帐单和其他问题。

2. 法律合规性：遵守所有目前颁布的资料保护法规，以防止诈欺和网络犯罪。

3. 安全与维护：保护基础设施以防止服务滥用；为合法使用者提供安全层，以降低未经授权存取的风险。

- 已认证状态展示：无选项卡及【去认证】按钮，仅展示认证状态（类型、时间）及相关操作按钮（按钮样式按次要按钮规范）。

### 前端交互逻辑（必须严格实现）

1.  页面加载时：优先调用接口查询认证状态，加载过程中显示平台统一加载动画（居中显示，覆盖当前页面，不可点击穿透）；

2.  选项卡切换：点击选项卡瞬间切换选中状态，【去认证】按钮同步关联当前选中选项卡，底部提示文案不切换；

3.  按钮交互：

- 【去认证】按钮：未选中选项卡时置灰不可点击；选中选项卡后可用，点击后触发对应认证须知弹窗（个人/企业区分）；

- 已认证状态操作按钮：按接口返回状态动态展示（未认证/审核中/已认证/未通过），点击后跳转至对应页面；

4.  异常处理：接口调用失败时，页面居中显示提示“获取认证状态失败，请刷新页面重试”，同时提供【刷新】按钮，点击重新调用接口。

### 前端校验（无表单校验，仅接口状态校验）

无前端表单校验，仅校验接口返回状态，按状态码对应展示页面内容（状态码详见“四、前端状态码及错误处理”）。

### 接口预留（仅前端调用，无需关注后端实现）

- 接口地址：`GET /api/auth/realname/status`（预留，后端调试完成后直接使用）；

- 请求方式：GET；

- 返回字段示例（前端按此格式解析，字段不可遗漏）：

```json

{
  "code": 0,
  "msg": "ok",
  "data": {
    "status": 0,      // 0-未认证 1-审核中 2-成功 3-失败
    "type": 1,        // 1-个人 2-企业
    "reason": "",     // 认证失败/驳回原因（非空仅当status=3时）
    "tencentCloudAuthId": "", // 个人认证成功后，返回腾讯云实名认证唯一标识（可选）
    "authCount": 0    // 当日个人认证提交次数（仅个人未认证/失败状态返回）
  }
}
    
```

### 开发注意事项

1.  页面加载时必须先调用接口，不可直接渲染默认未认证状态，避免数据不一致；

2.  选项卡切换需无卡顿，选中状态样式清晰（与未选中状态区分明显）；

3.  加载动画需与平台其他页面保持一致，不可自定义；

4.  已认证状态展示的时间格式，严格按“YYYY-MM-DD HH:MM:SS”渲染，不可修改格式。

---

## Page2 个人认证须知弹窗（前端核心开发项）

### 触发时机

点击个人认证选项卡下的【去认证】按钮后弹出，为个人认证前置必经步骤，不可跳过。

### 界面结构（严格对照UI设计稿）

- 弹窗容器：居中显示，宽度、高度按UI设计稿，添加半透明遮罩（遮罩透明度按UI规范，不可点击穿透）；

- 标题：个人实名认证须知（字体、颜色、间距按UI规范）；

- 滚动文本区域：高度固定300px，超出部分可滚动（滚动条样式按平台规范，不可自定义），文本内容不可修改：

身份认证通常情况下会迅速完成，但在特殊情况下，可能需要一些时间以便进行详细的审核。常见的拒绝原因为以下几点：

1. 假名字、2. 假身份证、3. 身份证照片模糊【图片示例】

请确保所有详细信息正确无误，以免造成任何干扰。感谢您的合作！

提示：您每天只有3次身份验证机会，如果验证被拒绝且达到次数限制，您将无法继续使用认证服务，需次日再试。

我们的合作服务提供者（腾讯云、微信/支付宝）负责身份验证，以确保资讯的安全性和保密性，Dataify和其他第三方无法存取您的身份信息。

- 复选框：“我同意使用我提供的照片来验证我的身份”（默认选中，不可取消，取消状态下【同意并继续】按钮置灰）；

- 底部按钮：【取消】【同意并继续】（按钮并排展示，间距按UI设计稿，样式分别对应次要按钮、主按钮规范）。

### 前端交互逻辑（必须严格实现）

1.  弹窗弹出：弹出时无动画，瞬间居中显示，遮罩同步显示；

2.  复选框交互：默认选中，点击取消后，【同意并继续】按钮置灰不可点击；再次点击选中，按钮恢复可用；

3.  按钮交互：

- 【取消】：点击后关闭弹窗，遮罩同步消失，返回实名认证入口页，保留个人认证选项卡选中状态，不清除任何数据；

- 【同意并继续】：仅复选框选中时可用，点击后关闭弹窗，跳转至个人认证信息填写页；

4.  滚动文本交互：滚动区域不可超出弹窗范围，滚动至底部后不可继续滚动，无回弹效果；

5.  弹窗关闭：仅可通过【取消】【同意并继续】按钮关闭，不可通过点击遮罩、ESC键关闭。

### 前端校验

仅校验复选框状态：未选中时，【同意并继续】按钮置灰，鼠标悬浮提示“请同意认证须知”，不可点击。

### 接口预留

无接口调用，仅页面跳转交互。

### 开发注意事项

1.  滚动文本区域高度固定300px，不可自适应，超出部分必须可滚动；

2.  复选框必须默认选中，且不可取消（逻辑限制，禁止用户取消勾选）；

3.  按钮吸底展示，不可被滚动文本遮挡，点击区域大小不小于48px*120px，确保点击体验；

4.  弹窗遮罩不可点击穿透，避免用户误操作后台页面。

---

## Page3 个人认证信息填写页（前端核心开发项，对接腾讯云前端交互）

### 界面结构（严格对照UI设计稿，核心开发点）

- 顶部标题：个人实名认证（字体、颜色、间距按UI规范）；

- 表单区域：包含3个必填字段，字段排列按UI设计稿，标签与输入框对齐，必填项标注“*”（红色）；

- 表单字段（按UI设计稿排列，不可调整顺序）：

|字段名|类型|前端展示及交互要求|
|---|---|---|
|姓氏 *|单行输入框|输入框占位提示“请输入姓氏”，输入时实时校验，错误提示显示在输入框下方（红色字体，间距按UI规范）|
|名字 *|单行输入框|输入框占位提示“请输入名字”，输入时实时校验，错误提示显示在输入框下方（红色字体，间距按UI规范）|
|身份证号码 *|单行输入框|输入框占位提示“请输入18位身份证号码，尾号X请大写”，输入时实时校验，错误提示显示在输入框下方（红色字体，间距按UI规范）|
- 提交次数提示：表单上方显示“今日剩余X次认证机会”（X为接口返回的3 - authCount），未认证/失败状态下显示，成功状态下隐藏；

- 底部按钮：【上一步】【提交】（按钮并排展示，间距按UI设计稿，样式分别对应次要按钮、主按钮规范）；

- 限制提示：当日提交次数达3次时，表单上方显示红色提示“超出每日认证限制，请联系客服”，【提交】按钮置灰。

### 前端交互逻辑（必须严格实现）

1.  页面加载：

- 加载时调用接口查询当日提交次数，显示加载动画（平台统一样式）；

- 若有暂存数据（前端暂存），自动填充至表单字段，无需用户重新输入；

2.  表单交互：

- 输入框聚焦时，边框高亮（按UI规范）；失焦时，触发实时校验；

- 输入过程中，实时校验输入内容，错误提示实时显示/隐藏；

- 身份证输入框支持自动大写（输入x时自动转为X），禁止输入空格；

3.  按钮交互：

- 【上一步】：点击后返回个人认证须知弹窗，保留当前表单已填写内容（前端暂存，无需调用接口）；

- 【提交】：仅所有表单字段校验通过、且当日提交次数<3时可用；点击后触发接口调用，调用过程中按钮置灰，显示加载动画，不可重复点击；

- 提交成功：接口返回成功后，自动弹出人脸核验二维码弹窗（跳转至Page4）；

- 提交失败：接口返回失败后，显示对应错误提示（红色字体，表单上方），按钮恢复可用，更新当日剩余提交次数；

4.  暂存逻辑：用户输入过程中，每30秒自动暂存一次表单数据（调用暂存接口），页面刷新后自动读取暂存数据，暂存数据24小时后自动清除；

5.  异常处理：

- 接口调用失败：显示提示“认证请求失败，请重试”，提供【重试】按钮，点击重新调用接口；

- 表单校验失败：输入框边框标红，错误提示显示在输入框下方，【提交】按钮置灰不可点击。

### 前端校验规则（必须严格实现，不可修改）

1.  姓氏校验：

- 为空 → 提示：请输入正确的姓氏（仅支持中文）；

- 输入非中文（含特殊字符、英文、数字） → 提示：请输入正确的姓氏（仅支持中文）；

- 长度超过8位 → 提示：姓氏长度不可超过8位；

2.  名字校验：

- 为空 → 提示：请输入正确的名字（仅支持中文）；

- 输入非中文（含特殊字符、英文、数字） → 提示：请输入正确的名字（仅支持中文）；

- 长度超过15位 → 提示：名字长度不可超过15位；

3.  身份证号码校验：

- 为空 → 提示：请输入有效的18位身份证号码（尾号X请大写）；

- 长度不足18位/超过18位 → 提示：请输入有效的18位身份证号码（尾号X请大写）；

- 格式错误（校验码错误、尾号非X/数字） → 提示：请输入有效的18位身份证号码（尾号X请大写）；

- 输入空格 → 自动去除空格，无需提示。

### 接口预留（仅前端调用，无需关注后端实现）

1.  前端暂存接口（可选，用于保存表单数据）：

- 接口地址：`POST /api/auth/realname/person/temp`；

- 请求方式：POST；

- 请求参数：{ surname: 姓氏, name: 名字, idCard: 身份证号码 }；

- 返回字段示例：{ "code": 0, "msg": "暂存成功", "data": {} }；

2.  腾讯云身份校验接口（触发人脸核验前置接口）：

- 接口地址：`POST /api/auth/realname/person/verify`；

- 请求方式：POST；

- 请求参数：{ surname: 姓氏, name: 名字, idCard: 身份证号码 }；

- 返回字段示例（成功）：{ "code": 0, "msg": "校验成功", "data": { "qrcodeUrl": "二维码地址", "verifyId": "核验唯一ID", "sessionId": "腾讯云会话标识" } }；

- 返回字段示例（失败）：{ "code": 1005, "msg": "认证失败：身份信息与上传信息不符，请核对并重新认证", "data": { "authCount": 1 } }；

3.  当日提交次数查询接口（页面加载时调用）：

- 接口地址：`GET /api/auth/realname/person/count`；

- 请求方式：GET；

- 返回字段示例：{ "code": 0, "msg": "ok", "data": { "authCount": 0 } }。

### 开发注意事项

1.  表单校验必须实时触发（聚焦、失焦、输入时均触发），错误提示样式、文案严格按要求，不可自定义；

2.  身份证输入框需支持自动大写尾号X，禁止输入空格，提升用户体验；

3.  暂存逻辑需实现自动暂存，避免用户刷新页面丢失数据；

4.  【提交】按钮需防止重复点击（点击后置灰+加载动画），避免多次调用接口；

5.  当日提交次数实时更新，达3次时，【提交】按钮置灰，提示文案显示在表单上方，颜色为红色；

6.  接口调用失败时，需提供重试按钮，不可让用户手动刷新页面；

7.  表单字段不可调整顺序、不可增减，严格按UI设计稿及本PRD要求实现。

---

## Page4 个人人脸核验页（前端核心开发项，对接腾讯云、微信/支付宝扫码）

### 界面结构（严格对照UI设计稿，核心开发项）

- 弹窗容器：居中显示，宽度、高度按UI设计稿，添加半透明遮罩（遮罩透明度按UI规范，不可点击穿透）；

- 顶部标题：人脸核验（腾讯云认证）（字体、颜色、间距按UI规范）；

- 提示文案：

二维码上方：打开手机，使用微信/支付宝扫描二维码（居中显示，字体按UI规范）；

二维码下方：您的信息由微信/支付宝审核，Dataify不保留任何身份信息，扫码过程中请勿关闭窗口或切换验证方式，验证通过会自动跳转（字体偏小，颜色为灰色，间距按UI设计稿）；

- 二维码区域：居中显示，大小按UI设计稿，清晰可扫，二维码下方标注“腾讯云人脸核验”标识（字体偏小，颜色为灰色）；

- 底部状态提示：居中显示，字体按UI规范，动态显示“等待核验中”“认证成功”“认证失败”“核验超时”；

- 无关闭按钮，不可手动关闭弹窗，仅可通过核验完成/超时自动关闭。

### 前端交互逻辑（必须严格实现）

1.  弹窗弹出：从个人认证信息填写页提交成功后，自动弹出，无动画，瞬间居中显示，遮罩同步显示；

2.  二维码渲染：接口返回qrcodeUrl后，立即渲染二维码，渲染失败时显示“二维码加载失败，请重试”，提供【重试】按钮，点击重新调用获取二维码接口；

3.  核验状态轮询：

- 二维码渲染成功后，开始轮询核验结果，每3秒调用一次轮询接口，不可频繁调用；

- 轮询过程中，底部状态显示“等待核验中”，无加载动画；

- 最多轮询20次（累计1分钟），超时后显示“核验超时，请重试”，5秒后自动关闭弹窗，返回个人认证信息填写页，不占用当日提交次数；

4.  核验结果处理：

- 核验成功（接口返回状态1）：底部状态显示“认证成功”，2秒后自动关闭弹窗，跳转至个人认证成功结果页；

- 核验失败（接口返回状态2）：底部状态显示“认证失败：身份信息与上传信息不符，请核对并重新认证”，5秒后自动关闭弹窗，返回个人认证信息填写页，更新当日剩余提交次数；

- 轮询超时：底部状态显示“核验超时，请重试”，5秒后自动关闭弹窗，返回个人认证信息填写页，不占用当日提交次数；

5.  异常处理：

- 二维码加载失败：显示提示+重试按钮，点击重试重新调用获取二维码接口；

- 轮询接口调用失败：显示“查询核验结果失败，请重试”，提供【重试】按钮，点击重新开始轮询；

- 不可手动关闭弹窗，不可通过点击遮罩、ESC键关闭，避免影响核验流程。

### 前端校验（无表单校验，仅状态校验）

仅校验接口返回的核验状态，按状态对应展示提示文案及跳转逻辑，无需额外前端校验。

### 接口预留（仅前端调用，无需关注后端实现）

1.  获取腾讯云三方实名认证二维码接口（个人认证信息填写页提交成功后调用）：

- 接口地址：`POST /api/auth/realname/person/face/qrcode`；

- 请求方式：POST；

- 请求参数：{ verifyId: "核验唯一ID", sessionId: "腾讯云会话标识" }；

- 返回字段示例（成功）：{ "code": 0, "msg": "ok", "data": { "qrcodeUrl": "二维码地址" } }；

- 返回字段示例（失败）：{ "code": 3004, "msg": "认证服务暂时不可用，请稍后重试", "data": {} }；

2.  核验结果轮询接口（二维码渲染成功后调用）：

- 接口地址：`GET /api/auth/realname/person/face/result?verifyId=xxx`；

- 请求方式：GET；

- 返回字段示例（处理中）：{ "code": 0, "msg": "ok", "data": { "status": 0 } }；

- 返回字段示例（成功）：{ "code": 0, "msg": "ok", "data": { "status": 1 } }；

- 返回字段示例（失败）：{ "code": 3001, "msg": "认证失败：身份信息与上传信息不符，请核对并重新认证", "data": { "status": 2, "authCount": 1 } }。

### 开发注意事项

1.  二维码渲染需清晰，不可拉伸、模糊，确保微信/支付宝可正常扫码；

2.  轮询策略严格按要求（每3秒一次，最多20次），不可修改轮询间隔及次数，避免接口异常；

3.  核验状态提示文案严格按要求，不可自定义，颜色、字体按UI规范；

4.  弹窗不可手动关闭，仅可通过核验完成/超时自动关闭，避免用户误操作；

5.  异常情况（二维码加载失败、轮询失败）需提供重试按钮，不可让用户手动刷新页面；

6.  跳转逻辑需及时响应，核验成功/失败后，按规定时间关闭弹窗并跳转，无卡顿；

7.  二维码下方“腾讯云人脸核验”标识需清晰显示，与提示文案区分开。

---

## Page5 企业认证须知弹窗（前端开发项）

### 触发时机

点击企业认证选项卡下的【去认证】按钮后弹出，为企业认证前置必经步骤，不可跳过。

### 界面结构（严格对照UI设计稿）

- 弹窗容器：居中显示，宽度、高度按UI设计稿，添加半透明遮罩（遮罩透明度按UI规范，不可点击穿透）；

- 标题：欢迎来到Dataify的企业验证流程（字体、颜色、间距按UI规范）；

- 操作按钮：【返回】（顶部右侧，次要按钮样式）；

- 文本内容（不可修改）：

对于所有需使用数据采集类产品或特殊需求的客户，本步骤为强制性要求。该流程将通过核验您的业务类型与使用场景，确保所有行为符合最高合规标准与行业道德规范。

我们已投入大量资源保障网络安全与系统完整性，并持续实施安全监控，确保为客户及合作伙伴提供稳定、可靠、安全的服务保障。

审批流程最长约 1–2 个工作日。如需协助，请通过邮件联系合规团队：compliance@dataify.com，我们将竭诚为您提供支持。

- 确认按钮：【继续】（底部居中，主按钮样式，宽度按UI设计稿）。

### 前端交互逻辑（必须严格实现）

1.  弹窗弹出：弹出时无动画，瞬间居中显示，遮罩同步显示；

2.  按钮交互：

- 【返回】：点击后关闭弹窗，遮罩同步消失，返回实名认证入口页，保留企业认证选项卡选中状态；

- 【继续】：点击后关闭弹窗，跳转至企业认证信息填写页；

3.  文本交互：文本不可编辑，超出弹窗高度时可滚动（滚动条样式按平台规范）；

4.  弹窗关闭：仅可通过【返回】【继续】按钮关闭，不可通过点击遮罩、ESC键关闭。

### 前端校验

无校验，仅按钮交互逻辑。

### 接口预留

无接口调用，仅页面跳转交互。

### 开发注意事项

1.  文本内容不可修改，换行、间距按UI设计稿；

2.  【返回】按钮位于顶部右侧，样式为次要按钮，不可修改位置及样式；

3.  弹窗遮罩不可点击穿透，避免用户误操作后台页面。

---

## Page6 企业认证信息填写页（前端开发项）

### 界面结构（严格对照UI设计稿，核心开发点）

- 顶部标题：企业实名认证（字体、颜色、间距按UI规范）；

- 表单区域：包含3个必填上传字段，字段排列按UI设计稿，标签与上传区域对齐，必填项标注“*”（红色）；

- 表单字段（按UI设计稿排列，不可调整顺序）：

|字段名|类型|前端展示及交互要求|提示文案（不可修改）|
|---|---|---|---|
|企业证件 *|文件上传|支持点击上传、拖拽上传，上传后显示缩略图、文件名、文件大小，支持删除重传|请上传公司的营业执照或公司注册证书的复印件，确保证件清晰、信息完整（无遮挡、无模糊），有效期在存续期内|
|公司银行账户信息 *|文件上传|支持点击上传、拖拽上传，上传后显示缩略图、文件名、文件大小，支持删除重传|请上传开户证明或月结单中的任意一项文件以完成认证，文件需清晰展示公司名称、银行账户信息|
|辅助信息*|文件上传|支持点击上传、拖拽上传，上传后显示缩略图、文件名、文件大小，支持删除重传|请上传水费账单、电费账单或租赁合同中的任意一项文件协助辅助信息，文件需清晰展示公司名称及相关地址信息|
- 上传限制提示：每个上传区域下方显示“格式支持jpg/png/pdf，最大支持20M”（灰色字体，间距按UI设计稿）；

- 底部按钮：【上一步】【提交审核】（按钮并排展示，间距按UI设计稿，样式分别对应次要按钮、主按钮规范）；

- 上传进度提示：文件上传过程中，显示进度条（按平台规范），进度条下方显示上传百分比。

### 前端交互逻辑（必须严格实现）

1.  页面加载：无接口调用，直接渲染表单，上传区域显示“点击或拖拽文件至此处上传”提示；

2.  上传交互：

- 点击上传：点击上传区域，弹出系统文件选择框，仅可选择jpg、png、pdf格式文件，文件大小超过20M时，提示“文件大小超出限制，最大支持20M”；

- 拖拽上传：拖拽文件至上传区域，文件格式不符时，提示“文件格式错误，请上传jpg/png/pdf格式文件”；文件大小超出限制时，提示“文件大小超出限制，最大支持20M”；

- 上传过程：显示进度条及上传百分比，进度条不可点击，上传过程中不可重复上传同一字段文件；

- 上传成功：显示文件缩略图（按比例缩放，不可拉伸）、文件名、文件大小，缩略图下方显示【删除】按钮（点击可删除文件，删除后可重新上传）；

- 上传失败：显示“文件上传失败，请重新上传”（红色字体），提供【重新上传】按钮，点击重新上传；

3.  按钮交互：

- 【上一步】：点击后返回企业认证须知弹窗，保留已上传的文件（前端暂存，无需调用接口）；

- 【提交审核】：仅3个字段均上传成功（无删除）后，按钮由置灰变为可用；点击后触发接口调用，调用过程中按钮置灰，显示加载动画，不可重复点击；

- 提交成功：接口返回成功后，跳转至企业认证提交成功结果页；

- 提交失败：接口返回失败后，显示对应错误提示（红色字体，表单上方），按钮恢复可用，已上传文件不丢失；

4.  异常处理：

- 接口调用失败：显示提示“提交失败，请重试”，提供【重试】按钮，点击重新调用接口；

- 文件上传失败：每个上传区域下方显示错误提示，点击重新上传可再次尝试；

- 浏览器不支持拖拽上传：隐藏拖拽提示，仅保留点击上传功能，不提示用户。

### 前端校验规则（必须严格实现，不可修改）

1.  文件格式校验：仅允许上传jpg、png、pdf格式，其他格式提示“文件格式错误，请上传jpg/png/pdf格式文件”；

2.  文件大小校验：单个文件最大20M，超出限制提示“文件大小超出限制，最大支持20M”；

3.  必填校验：3个字段均需上传文件，未上传时，【提交审核】按钮置灰，鼠标悬浮提示“请完成所有必填项上传”；

4.  重复上传校验：同一字段已上传文件未删除时，不可重复上传，点击上传区域提示“已上传文件，请先删除后重新上传”。

### 接口预留（仅前端调用，无需关注后端实现）

1.  文件上传接口：

- 接口地址：`POST /api/upload`；

- 请求方式：POST；

- 请求参数：formData格式（file: 上传的文件）；

- 返回字段示例（成功）：{ "code": 0, "msg": "上传成功", "data": { "fileUrl": "文件地址", "fileId": "文件唯一标识" } }；

- 返回字段示例（失败）：{ "code": 2002, "msg": "文件上传失败，请重新上传", "data": {} }；

2.  提交企业认证接口：

- 接口地址：`POST /api/auth/realname/company/submit`；

- 请求方式：POST；

- 请求参数：{ fileIds: [fileId1, fileId2, fileId3] }；

- 返回字段示例（成功）：{ "code": 0, "msg": "提交成功", "data": {} }；

- 返回字段示例（失败）：{ "code": 2002, "msg": "材料模糊/缺失，请重新上传", "data": {} }。

### 开发注意事项

1.  上传区域提示文案严格按要求，不可修改，拖拽上传功能需兼容主流浏览器；

2.  文件上传进度条需实时更新，上传百分比准确，不可卡顿；

3.  已上传文件的缩略图按比例缩放，不可拉伸、模糊，【删除】按钮点击后需确认（弹出确认弹窗“确定删除该文件？”，包含【确定】【取消】按钮）；

4.  【提交审核】按钮仅在所有字段上传完成后可用，不可提前开放；

5.  接口调用失败时，已上传文件不丢失，避免用户重新上传；

6.  文件格式、大小校验需在前端提前校验，避免无效接口调用；

7.  表单字段不可调整顺序、不可增减，严格按UI设计稿及本PRD要求实现。

---

## Page7 认证结果页（前端核心开发项）

### 界面结构（严格对照UI设计稿，核心开发点）

- 顶部标题：认证结果（字体、颜色、间距按UI规范）；

- 状态区域：居中显示，包含图标、文案、按钮，按认证状态动态渲染（5种状态，不可遗漏）；

- 5种状态具体展示（严格按以下文案，不可修改）：

1.  个人认证成功：

- 图标：绿色对勾（按UI设计稿，大小固定）；

- 文案：您的实名认证已认证通过（腾讯云认证通过，由微信/支付宝完成核验）。（居中显示，字体按UI规范）；

- 按钮：【返回首页】（主按钮样式，居中显示）；

2.  个人认证未通过：

- 图标：红色叉号（按UI设计稿，大小固定）；

- 文案：认证失败：身份信息与上传信息不符，请核对并重新认证（腾讯云校验失败）。（若已达每日3次限制，额外补充：超出每日认证限制，请联系客服）（居中显示，红色字体）；

- 按钮：【重新提交】（主按钮样式）、【联系客服经理】（次要按钮样式）、【提交人工审核】（次要按钮样式），并排居中显示；

3.  企业认证提交成功：

- 图标：灰色旋转加载图标（按UI设计稿，大小固定，加载动画按平台规范）；

- 文案：已提交审核，1-2 个工作日内完成。如需协助，请联系合规团队：compliance@dataify.com（居中显示，字体按UI规范）；

- 按钮：【查看状态】（主按钮样式，居中显示）；

4.  企业认证通过：

- 图标：绿色对勾（按UI设计稿，大小固定）；

- 文案：您的企业认证已通过审核。（居中显示，字体按UI规范）；

- 按钮：【返回首页】（主按钮样式，居中显示）；

5.  企业认证未通过：

- 图标：红色叉号（按UI设计稿，大小固定）；

- 文案：您的企业认证未通过，请在此点击重新提交，如在认证中遇到问题请联系您的客服经理。或提交个人实名认证。（居中显示，红色字体）；

- 按钮：【重新提交】（主按钮样式）、【联系客服经理】（次要按钮样式）、【提交个人实名认证】（次要按钮样式），并排居中显示。
> （注：文档部分内容可能由 AI 生成）